{
  "name": "@ecoprompt/ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "Copilot chat interception (transcript + chatSession tail), manual & scripted adapters, usage providers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@ecoprompt/shared-types": "*",
    "@ecoprompt/scoring-engine": "*",
    "chokidar": "^4.0.3"
  }
}
